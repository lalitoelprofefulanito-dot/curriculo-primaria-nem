# Trazabilidad

Los registros de PDAS y proyectos conservan fuente, hoja y fila de origen.
Las relaciones provenientes de `Libros` conservan su fila.
Las discrepancias se conservan en `auditoria/`.

Ruta:
`repositorio → registro_id → fuente → hoja → fila → Excel original`
